# cs332_lab05

Open a terminal in the directory containing readdir_v2.c and compile the program:

gcc -Wall -o readdir_recursive readdir_v2.c
Run

Run the program by providing a directory name

Example:

./readdir_recursive .

The program will recursively traverse the specified directory and display all files and subdirectories.